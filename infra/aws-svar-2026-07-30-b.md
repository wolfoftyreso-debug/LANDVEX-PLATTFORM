# Landvex AWS-svar — 2026-07-30 (del b: åtgärder)

*Sammanfattning inklistrad i konversationen av användaren; agentens
fullständiga fil (`landvex-aws-svar-2026-07-30-b.md`, 12 KB) med full
terminalutskrift finns hos användaren/AWS-agenten, inte återgiven
ordagrant här. Det här dokumentet fångar de fakta som faktiskt
förändrade koden i det här repot.*

## Punkt 1 — Server-2 ✅

- Kod uppdaterad till `a2f50d73309618b168e221f4f851bc659775e215`
  (`wolfoftyreso-debug/LANDVEX-PLATTFORM`, gren `main`), tjänsten uppe,
  `/health` svarar 200, `engine_version` 1.1.0.
- AAMOS-token förnyat: `scripts.aamos_smoke`-proben gick från 401 till
  "Reachable".
- **Bekräftad aktiv quiXzoom-route: `/api/quixzoom/missions`** — inte
  `/api/qz/missions`, som stod som gissat default i
  `integrations/aamos.py` och i `/etc/landvex/opportunity.env`.
  Åtgärdat i det här repot: `integrations/aamos.py`s default,
  `deploy/k8s/configmap.yaml`, `scripts/aamos_smoke.py`s kandidatlista,
  samt dokumentation (`CLAUDE.md`, `docs/aws-deployment.md`,
  `docs/dev-sync.md`, `infra/landvex-opportunity.service`) och
  tester (`tests/test_aamos.py`, `tests/test_scb.py`,
  `engine/stack.py`, `engine/datasources/adapters.py`,
  `engine/datasources/quixzoom_direct.py`).
- `scripts/migrate_sqlite_to_postgres.py` bekräftat på plats på
  server-2 och importerar rent.

## Punkt 2 — Kubernetes ✅ (alla fem luckor från docs/k8s.md stängda)

1. OIDC-federation — aktiverad på `landvex-prod`.
2. AWS Load Balancer Controller — 2/2 pods Running.
3. ECR-repo `landvex/opportunity-engine` — skapat.
4. RDS `landvex_opportunity` i EKS-VPC:t — available, endpoint klart
   (namnet matchar avsiktligt INTE den främmande `landvex`-databasen
   på server-2, se `docs/k8s.md` beslut D2).
5. ACM-certifikat för `opportunity.landvex.com` — ISSUED.

## Nästa blockers (från agentens svar, ej ännu gjorda)

- Skapa `landvex-secrets` i klustret (`kubectl create secret generic
  landvex-secrets -n landvex ...`, se `docs/k8s.md` §3).
- Bygg och pusha imagen till det nya ECR-repot.
- `kubectl apply -k deploy/k8s`.
