apiVersion: v1
kind: Service
metadata:
  name: landvex-opportunity-engine
  namespace: landvex
spec:
  type: ClusterIP
  selector:
    app: landvex-opportunity-engine
  ports:
    - name: http
      port: 80
      targetPort: http
